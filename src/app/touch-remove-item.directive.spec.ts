import { TouchRemoveItemDirective } from './touch-remove-item.directive';

describe('TouchRemoveItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchRemoveItemDirective();
    expect(directive).toBeTruthy();
  });
});
